port=2000
timezone=Asia/Jakarta

# Redis is optional for local dev. Leave unset or anything other than `redis=true` to skip Redis.
# When redis=true, gateway degrades to No-Cache mode if the server is unreachable (see account/core/utils/redis.go).
# redis=true
# redis_host=127.0.0.1
# redis_port=6379